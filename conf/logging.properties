# JUL configuration — loaded at JVM startup, before any application class.
# Suppresses Lucene's vector incubator warning (harmless on JDK 24+ where
# the Vector API lives in java.base and the incubator module is gone).
org.apache.lucene.level=SEVERE
